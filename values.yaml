registry:
  enabled: true
  image:
    registry: "quay.io"
    repository: "apicurio/apicurio-registry-mem"
    tag: "2.0.0.Final"
  resources: {}
    # limits:
    #   cpu: 1000m
    #   memory: 1300Mi
    # requests:
    #   cpu: 200m
    #   memory: 600Mi

sync:
  enabled: true
  image:
    registry: "quay.io"
    repository: "apicurio/apicurio-registry-kube-sync"
    tag: "latest-snapshot"
  # registryUrl:
  # watchNamespaces:
  resources: {}
    # limits:
    #   cpu: 500m
    #   memory: 800Mi
    # requests:
    #   cpu: 200m
    #   memory: 600Mi

